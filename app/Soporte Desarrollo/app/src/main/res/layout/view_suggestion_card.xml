<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/card_margin_h"
    android:layout_marginEnd="@dimen/card_margin_h"
    android:layout_marginBottom="@dimen/card_margin_bottom"
    android:background="@drawable/card_background"
    android:elevation="@dimen/elevation_floating"
    android:orientation="vertical"
    android:padding="@dimen/card_padding">

    <!-- Label -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/suggestion_label"
        android:textSize="@dimen/text_caption"
        android:textColor="@color/brand_primary"
        android:fontFamily="sans-serif-medium"
        android:textAllCaps="true"
        android:letterSpacing="0.1"
        android:layout_marginBottom="@dimen/space_sm" />

    <!-- Item name -->
    <TextView
        android:id="@+id/tv_item_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Item Name"
        android:textSize="@dimen/text_heading"
        android:textColor="@color/text_primary"
        android:fontFamily="sans-serif-medium"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginBottom="@dimen/space_xs" />

    <!-- Price -->
    <TextView
        android:id="@+id/tv_item_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="$0.00"
        android:textSize="@dimen/text_subheading"
        android:textColor="@color/accent"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="@dimen/space_md" />

    <!-- Reason / script for staff -->
    <TextView
        android:id="@+id/tv_reason"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Reason text here"
        android:textSize="@dimen/text_secondary_size"
        android:textColor="@color/text_secondary"
        android:fontStyle="italic"
        android:lineSpacingMultiplier="1.3"
        android:layout_marginBottom="@dimen/space_lg" />

    <!-- Buttons row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btn_dismiss"
            android:layout_width="0dp"
            android:layout_weight="1"
            style="@style/ButtonDismiss"
            android:text="@string/dismiss"
            android:layout_marginEnd="@dimen/space_sm" />

        <Button
            android:id="@+id/btn_add"
            android:layout_width="0dp"
            android:layout_weight="2"
            style="@style/ButtonPrimary"
            android:text="@string/add_to_order" />

    </LinearLayout>

</LinearLayout>
